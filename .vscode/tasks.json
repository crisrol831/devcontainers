{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "java_dev",
            "type": "shell",
            "command": "docker-compose up -d java_dev"
        },
        {
            "label": "js_dev",
            "type": "shell",
            "command": "docker-compose up -d js_dev"
        },
        {
            "label": "py_dev",
            "type": "shell",
            "command": "docker-compose up -d py_dev"
        },
        {
            "label": "sf_dev",
            "type": "shell",
            "command": "docker-compose up -d sf_dev"
        }
    ]
}